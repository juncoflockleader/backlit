# Apps

Application-level shell pieces live here when they grow beyond compositor-adjacent crates.

- `launcher`: keyboard-first app launcher.
- `settings`: display/input/appearance/power settings UI.
- `terminal-wrapper`: stable command path for launching the selected terminal.

