[Unit]
Description=Backlit shell
After=backlit-compositor.service
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/backlit-shell --component=all --socket=backlit-0
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
